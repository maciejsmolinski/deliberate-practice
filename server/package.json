{
  "name": "server",
  "private": true,
  "version": "1.0.0",
  "description": "A server of the deliberate practice board application",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "ts-node index.ts",
    "watch": "nodemon -w *.ts -i node_modules --exec \"npx ts-node index.ts\"",
    "lint:ts": "tslint **/*.{ts,tsx}"
  },
  "dependencies": {
    "apollo-server": "^2.19.0",
    "graphql": "^15.4.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.6",
    "ts-node": "^9.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.0.5"
  }
}
